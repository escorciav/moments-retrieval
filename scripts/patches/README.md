Keep relevant changes to our codebase for which we have hope, but somehow things didn't go as we expected.

# Importance sampling

Using importance sampling a.k.a. hard-mining over N_inter and N_intra at the moment level.

At some point, we added into our codebase (`c470b59f7117ba987a6afd2fce6e82f6b36aec88`). Yes, we forgot a bit about the linear history during the deadline 😅.

`intra-inter-importance-sampling`
